options:
  env:
    - "GOPATH=/go"
  machineType: 'E2_HIGHCPU_8'
steps:
  - id: go_test
    name: "gcr.io/cloud-builders/go"
    args: ["test","./..."]
    env: ["GOPATH=."]

timeout: 1800s
logsBucket: '${_CLOUD_BUILD_STORAGE_BUCKET}'
